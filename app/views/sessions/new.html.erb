<% provide(:title, 'Log in') %>
<h1>Log in</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(:session, url: login_path) do |f| %>

        <%= f.label :email %>
        <%= f.email_field :email, id: 'email', class: 'form-control' %>

        <%= f.label :password %>
        <%= link_to '(forgot password)', new_password_reset_path %>
        <%= f.password_field :password, id:'password', class: 'form-control' %>
        <%= f.label :remember_me, class: 'checkbox inline' do %>
        <%= f.check_box :remember_me %>
            <span>Remember me on this computer</span>
        <% end %>

        <%= f.submit 'Log in', id: 'log_in',class: 'btn btn-primary' %>
    <% end %>
    <p>New user? <%= link_to 'Sign path' %></p>
    <button class="btn" onclick="promo_login()">
    <!--<button class="btn" onclick="$('#promo-account').toggle();">-->
      Try promo account
    </button>
      <div id="promo-account" class="col-md-6 col-md-offset-3" style="display: none">
        <ul class="list-group">
          <li class="list-group-item">login: veimar.94@gmail.com</li>
          <li class="list-group-item">Admin password: password</li>
        </ul>
      </div>
  </div>

</div>

